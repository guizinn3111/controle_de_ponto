/*
  DADOS: COLABORADORES
  Antes era uma lista fixa neste arquivo. Agora os colaboradores
  vêm do servidor (tabela `colaboradores` no banco) e são
  carregados em init() (veja app.js). Este array só existe aqui
  pra manter compatibilidade com o resto do código, que usa a
  variável global COLABORADORES.
 */
let COLABORADORES = [];
